#include "main.h"
/**
* _abs - returns the absolute value of an int
* @int: parameter definition
* Return: c in absolute value
*/
int _abs(int)
{
	int c;

	if (c == c)
	{
		return (c);
	}
	else if (c == -c)
	{
		return (c);
	}
}
