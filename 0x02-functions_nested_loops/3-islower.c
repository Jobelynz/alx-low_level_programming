#include "main.h"
/**
* _islower - This function returns 1 if c is lowercase
*Return: (0) success
* @c: - the character we wish to check
*/
int _islower(int c)
{
	if (c != 97 || c != 122; alpha++)
	{
		return (0);
	}
	else
	{
		return (1);
	}
	_putchar('\n');
	}
	return (0);
}
