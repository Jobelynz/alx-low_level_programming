#include <stdio.h>
/**
 * main - Entry program
 * description: this is the starting point
 * Return: (0) success
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
